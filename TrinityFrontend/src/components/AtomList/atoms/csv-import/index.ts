import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const csvImport: Atom = {
  id: 'csv-import',
  title: 'CSV Import',
  category: 'Data Sources',
  description: 'Import data from CSV files with automatic type detection',
  tags: ['import', 'csv', 'data'],
  color: 'bg-blue-500'
};

export default csvImport;
