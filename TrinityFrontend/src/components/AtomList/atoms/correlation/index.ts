import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const correlation: Atom = {
  id: 'correlation',
  title: 'Correlation',
  category: 'Analytics',
  description: 'Calculate correlation between variables',
  tags: ['correlation', 'analysis', 'relationships'],
  color: 'bg-purple-500'
};

export default correlation;
