import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const jsonImport: Atom = {
  id: 'json-import',
  title: 'JSON Import',
  category: 'Data Sources',
  description: 'Load structured data from JSON files or APIs',
  tags: ['import', 'json', 'api'],
  color: 'bg-blue-500'
};

export default jsonImport;
