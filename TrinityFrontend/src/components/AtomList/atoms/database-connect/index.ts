import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const databaseConnect: Atom = {
  id: 'database-connect',
  title: 'Database Connect',
  category: 'Data Sources',
  description: 'Connect to SQL databases and execute queries',
  tags: ['database', 'sql', 'connect'],
  color: 'bg-blue-500'
};

export default databaseConnect;
