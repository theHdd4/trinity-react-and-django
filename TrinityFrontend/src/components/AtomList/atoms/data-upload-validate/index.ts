import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const dataUploadValidate: Atom = {
  id: 'data-upload-validate',
  title: 'Data Upload & Validate',
  category: 'Data Sources',
  description: 'Upload and validate data with automatic type detection',
  tags: ['upload', 'validate', 'data'],
  color: 'bg-blue-500'
};

export default dataUploadValidate;
