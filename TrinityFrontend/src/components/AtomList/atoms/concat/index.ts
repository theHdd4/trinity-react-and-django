import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const concat: Atom = {
  id: 'concat',
  title: 'Concat',
  category: 'Data Processing',
  description: 'Concatenate datasets vertically or horizontally',
  tags: ['concat', 'combine', 'append'],
  color: 'bg-green-500'
};

export default concat;
