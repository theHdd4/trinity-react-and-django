import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const atomMaker: Atom = {
  id: 'atom-maker',
  title: 'Atom maker',
  category: 'Utilities',
  description: 'Create custom atoms for specific tasks',
  tags: ['custom', 'atom', 'creation'],
  color: 'bg-gray-500'
};

export default atomMaker;
