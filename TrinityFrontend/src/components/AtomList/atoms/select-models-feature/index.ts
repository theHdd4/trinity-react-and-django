import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const selectModelsFeature: Atom = {
  id: 'select-models-feature',
  title: 'Select models - Feature based',
  category: 'Machine Learning',
  description: 'Select best models for feature-based predictions',
  tags: ['model', 'selection', 'features'],
  color: 'bg-orange-500'
};

export default selectModelsFeature;
