import type { Atom } from '@/components/AtomCategory/data/atomCategories';

const buildModelFeatureBased: Atom = {
  id: 'build-model-feature-based',
  title: 'Build model - feature based',
  category: 'Machine Learning',
  description: 'Build comprehensive feature-based models',
  tags: ['build', 'model', 'features'],
  color: 'bg-orange-500'
};

export default buildModelFeatureBased;
